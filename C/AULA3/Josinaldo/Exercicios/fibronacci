#include <stdio.h>

main() {
    long int num1 = 0, num2 = 1, num3;  
	int i, numr;
	
    printf("Entre com o numero de elementos: ");
    scanf("%d", &numr); 
	printf("%d %d ", num1, num2);
	for (i = 3; i <= numr; i++) {
		num3 = num1 + num2;
		printf("%d ", num3);
		num1 = num2;
		num2 = num3;
	}
}
